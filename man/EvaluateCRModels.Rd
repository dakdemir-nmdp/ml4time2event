% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_evaluation.R
\name{EvaluateCRModels}
\alias{EvaluateCRModels}
\title{Evaluate competing risks models on held-out data}
\usage{
EvaluateCRModels(
  models,
  data,
  timevar = NULL,
  eventvar = NULL,
  eval_times = NULL,
  prediction_times = NULL,
  ensemble_method = "average",
  cause = 1
)
}
\arguments{
\item{models}{A fitted survival model, a `SurvEnsemble`, or an
`ml4t2e_pipeline` with `analysis_type = "survival"`.}

\item{data}{Evaluation data frame containing the time and event columns used
during training.}

\item{timevar}{Name of the time-to-event column. Optional when `models`
already stores this information (e.g., `SurvEnsemble`, pipeline).}

\item{eventvar}{Name of the event indicator column (0 = censored, 1 = event).
Optional when stored within `models`.}

\item{eval_times}{Numeric vector of times at which to summarise the Brier
score. Defaults to the prediction grid used for scoring.}

\item{prediction_times}{Numeric vector of times passed to the prediction
routine. Defaults to `eval_times` when supplied, otherwise a grid spanning
the evaluation data.}

\item{ensemble_method}{Ensemble strategy forwarded to [PredictSurvModels()]
(ignored for single models). Defaults to `"average"`.}

\item{cause}{Integer code of the event of interest (default `1`).}
}
\value{
A leaderboard data frame with the same columns described for
  [EvaluateSurvModels()], ranked by integrated Brier score.
}
\description{
Computes cumulative-incidence accuracy diagnostics (Brier scores and
concordance indices) for single competing-risks models, `CREnsemble`
objects, or competing-risks pipelines.
}
