% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/core_evaluate.R
\name{ml4t2e_evaluate}
\alias{ml4t2e_evaluate}
\title{Evaluate fitted models on metrics}
\usage{
ml4t2e_evaluate(
  fit_or_preds,
  task = NULL,
  metrics = c("c_index", "ibs"),
  include = "all"
)
}
\arguments{
\item{fit_or_preds}{A \code{t2e_fit} object or predictions returned by
\code{predict.t2e_fit()}.}

\item{task}{Optional task providing ground truth if \code{fit_or_preds} contains
predictions on new data.}

\item{metrics}{Character vector of metrics to compute. Default: \code{c("c_index", "ibs")}.}

\item{include}{Which models to include (same semantics as \code{predict()}).}
}
\value{
A tibble with columns \code{model}, \code{metric}, and \code{value}.
}
\description{
Computes prediction performance metrics on a task. Currently supports:
\itemize{
\item \strong{Concordance Index (C-index)}: Harrell's C-statistic measuring discrimination.
For survival: \eqn{C = \frac{\sum_{i,j} I(t_i < t_j) \cdot I(\hat{S}_i > \hat{S}_j) \cdot \delta_j}{\sum_{i,j} I(t_i < t_j) \cdot \delta_j}}
where \eqn{\delta_j} is event indicator, \eqn{\hat{S}_i} is predicted survival probability.
Range: 0.5 (random) to 1.0 (perfect).
}
}
\details{
\itemize{
\item \strong{Integrated Brier Score (IBS)}: Mean squared difference between predicted
and observed outcomes over time.
\eqn{IBS = \frac{1}{t_{max} - t_{min}} \int_0^{t_{max}} BS(t) dt}
where \eqn{BS(t) = E[(S(t|X) - Y(t))^2]} for survival data.
Range: 0 (perfect) to 1 (worst).
}

For competing risks, C-index is computed for each cause separately using
time-dependent concordance, and Brier score uses cumulative incidence functions.
}
\references{
Harrell, F. E., Lee, K. L., & Mark, D. B. (1996).
"Multivariable prognostic models: Issues in developing models, evaluating
assumptions and adequacy, and measuring and reducing errors."
\emph{Statistics in Medicine}, 15(4), 361–387.
}
\keyword{internal}
