% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_wrappers.R
\name{ml4t2e_fit_pipeline}
\alias{ml4t2e_fit_pipeline}
\title{Fit a time-to-event pipeline (backwards-compatible helper)}
\usage{
ml4t2e_fit_pipeline(
  data,
  analysis_type = c("survival", "competing_risks"),
  timevar,
  eventvar,
  causevar = NULL,
  expvars = NULL,
  idvars = character(0),
  recipe_fn = minimal_data_recipe,
  recipe_args = list(pmiss = 0.25, pother = 0.05, dummy = FALSE),
  prediction_times = NULL,
  models = NULL,
  ensemble = "auto",
  controls = list(),
  resampling = NULL,
  metrics = NULL,
  include_rf = TRUE,
  ntreeRF = NULL,
  varsel = NULL,
  ...
)
}
\arguments{
\item{data}{Training data frame.}

\item{analysis_type}{\code{"survival"} or \code{"competing_risks"}.}

\item{timevar}{Name of the time column.}

\item{eventvar}{Name of the event/status column.}

\item{causevar}{Optional cause column for competing-risk tasks. When
\code{NULL}, event codes from \code{eventvar} are reused for causes.}

\item{expvars}{Optional predictor set (defaults to all non-outcome columns).}

\item{idvars}{Optional identifier column (first entry is used).}

\item{recipe_fn}{Optional preprocessing function (defaults to
\code{minimal_data_recipe()}); set to \code{NULL} to skip recipes.}

\item{recipe_args}{Named list of additional arguments for \code{recipe_fn}.}

\item{prediction_times}{Optional numeric time grid forwarded to \code{controls$times}.}

\item{models}{Character vector of engine names (legacy aliases are supported).}

\item{ensemble}{Ensemble strategy passed to \code{ml4t2e_fit()}.}

\item{controls}{Optional named list of per-engine controls.}

\item{resampling}{Optional \code{rsample} resampling object.}

\item{metrics}{Optional metric identifiers for \code{ml4t2e_evaluate()}.}

\item{include_rf, ntreeRF, varsel}{Legacy arguments retained for compatibility.
They are ignored (with a warning when changed from defaults).}

\item{...}{Unused; retained for compatibility.}
}
\value{
A fitted \code{T2EPipeline} object (also inheriting the
\code{ml4t2e_pipeline} class for S3 helpers).
}
\description{
This convenience wrapper bridges the legacy \code{ml4t2e_fit_pipeline()} workflow
onto the new \code{ml4t2e_pipeline()} + \code{T2EPipeline} infrastructure. It accepts
the original argument names but internally builds a modern pipeline,
returning an object with classes \code{c("ml4t2e_pipeline", "T2EPipeline", "R6")}.
}
\keyword{internal}
