% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline_wrappers.R
\name{ml4t2e_fit_pipeline}
\alias{ml4t2e_fit_pipeline}
\title{Fit an ml4time2event modeling pipeline}
\usage{
ml4t2e_fit_pipeline(
  data,
  analysis_type = c("survival", "competing_risks"),
  timevar,
  eventvar,
  expvars = NULL,
  idvars = character(0),
  recipe_fn = minimal_data_recipe,
  recipe_args = list(pmiss = 0.25, pother = 0.05, dummy = FALSE),
  prediction_times = NULL,
  models = NULL,
  ntreeRF = 300,
  varsel = FALSE,
  include_rf = TRUE,
  ...
)
}
\arguments{
\item{data}{Training data frame.}

\item{analysis_type}{Either `"survival"` or `"competing_risks"`.}

\item{timevar}{Name of time-to-event column.}

\item{eventvar}{Name of event indicator column (0/1 for survival, 0/1/2... for CR).}

\item{expvars}{Optional character vector of predictors. Defaults to all columns
except outcomes and IDs.}

\item{idvars}{Optional character vector of identifier columns that should pass
through preprocessing unchanged.}

\item{recipe_fn}{Preprocessing function accepting `model_recipe` as first argument
(defaults to [minimal_data_recipe()]).}

\item{recipe_args}{Named list of additional arguments passed to `recipe_fn`.}

\item{prediction_times}{Optional numeric vector specifying the default time grid
for downstream predictions. When `NULL`, the grid spans the observed follow-up.}

\item{models}{Optional character vector passed to [RunSurvModels()] or
[RunCRModels()]. When `NULL`, sensible defaults are used.}

\item{ntreeRF}{Number of trees for random forest models (forwarded to Run* helpers).}

\item{varsel}{Logical flag forwarded to [RunCRModels()] indicating variable selection.}

\item{include_rf}{Logical; if `FALSE`, skips fitting the baseline random forest
models within [RunSurvModels()] and [RunCRModels()].}

\item{...}{Additional arguments forwarded to [RunSurvModels()] or [RunCRModels()].}
}
\value{
An object of class `ml4t2e_pipeline` containing the fitted preprocessing
  recipe, ensemble model, and metadata.
}
\description{
Convenience wrapper that combines preprocessing, model fitting,
persistence, and prediction for survival or competing-risks analyses.
}
